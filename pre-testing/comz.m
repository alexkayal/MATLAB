g = ZMatrix;
tot = 0;

ZMatrix = g(1:20,:);
testotp;
tot = tot + max(profit);
plot (profit,'-blue');
hold on;

ZMatrix = g(21:40,:);
testotp;
tot = tot + max(profit);
plot (profit,'-green');
hold on;

ZMatrix = g(41:60,:);
testotp;
tot = tot + max(profit);
plot (profit,'-red');
hold on;

ZMatrix = g(61:80,:);
testotp;
tot = tot + max(profit);
plot (profit,'-black');
hold on;

ZMatrix = g(81:100,:);
testotp;
tot = tot + max(profit);
plot (profit,'-blue');
hold on;

ZMatrix = g(101:120,:);
testotp;
tot = tot + max(profit);
plot (profit,'-green');
hold on;

ZMatrix = g(121:140,:);
testotp;
tot = tot + max(profit);
plot (profit,'-red');
hold on;

ZMatrix = g(141:160,:);
testotp;
tot = tot + max(profit);
plot (profit,'-black');
hold on;

ZMatrix = g(161:180,:);
testotp;
tot = tot + max(profit);
plot (profit,'-blue');
hold on;

ZMatrix = g(181:200,:);
testotp;
tot = tot + max(profit);
plot (profit,'-green');
hold on;

ZMatrix = g(201:220,:);
testotp;
tot = tot + max(profit);
plot (profit,'-red');
hold on;

ZMatrix = g(221:240,:);
testotp;
tot = tot + max(profit);
plot (profit,'-black');
hold on;

ZMatrix = g(241:260,:);
testotp;
tot = tot + max(profit);
plot (profit,'-blue');
hold on;

ZMatrix = g(261:280,:);
testotp;
tot = tot + max(profit);
plot (profit,'-green');
hold on;

ZMatrix = g(281:300,:);
testotp;
tot = tot + max(profit);
plot (profit,'-red');
hold on;

ZMatrix = g(301:319,:);
testotp;
tot = tot + max(profit);
plot (profit,'-black');
hold on;